import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    name: 'x-star-query',
  },
  base: '/x-star-query/',
  outputPath: 'docs-dist',
  publicPath: '/x-star-query/',
});
